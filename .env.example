# Copy to .env for local production-style experiments.
# Do not commit real API keys, SMTP credentials, or payment provider secrets.

DRY_RUN=true
EMAIL_PROVIDER=sendgrid
EMAIL_FROM=finance@example.com
SENDGRID_API_KEY=replace_with_real_key_outside_git
FINANCE_REVIEW_EMAIL=finance-manager@example.com
